const PREFIX = {
  API: '/api',
};

const ENDPOINT = {
  CATEGORY: '/category',
  IMAGE: '/image',
  POST: '/post',
  'POST-PREVIEW': '/postPreview',
  SERIES: '/series',
  TAG: '/tag',
};

const DETAIL = {
  VERSION: '/version',
};

export { PREFIX, ENDPOINT, DETAIL };
