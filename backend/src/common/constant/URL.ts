const PREFIX = {
  API: '/api',
};

const ENDPOINT = {
  CATEGORY: '/category',
  IMAGE: '/image',
  POST: '/post',
  SERIES: '/series',
  TAG: '/tag',
};

const BEHAVIOR = {
  NEW: '/new',
  NEW_VERSION: '/new-version',
};

export { PREFIX, ENDPOINT, BEHAVIOR };
