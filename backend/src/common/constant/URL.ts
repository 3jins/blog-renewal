const PREFIX = {
  API: '/api',
};

const ENDPOINT = {
  CATEGORY: '/category',
  IMAGE: '/image',
  POST: '/post',
  SERIES: '/series',
  TAG: '/tag',
};

const BEHAVIOR = {
  NEW: '/new',
};

export { PREFIX, ENDPOINT, BEHAVIOR };
